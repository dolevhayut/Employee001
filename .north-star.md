<north-star>
GOAL: EmployeeX is now a SEPARATE PRODUCT (own repo), not a mode — per docs/local/DECISION-EMPLOYEEX-SPLIT.md. Employee001 keeps the toggle (renamed "Autonomy") + Tier 1/2 plumbing as twin governance.
USER: Dolev, solo founder.
DONE-WHEN (next phase): toggle rebranded to "Autonomy" in Employee001; new employeex repo scaffolded with the 5 copied core libs; EmailProvider interface + identity spike done; v0.1 vertical slice = one real customer email answered autonomously with full audit.
NON-GOALS:
  - Browser-per-agent, multi-tenancy, org chart, extra channels (v0.1 hard non-goals)
  - Shared npm package for the core — COPY, don't extract (revisit only when double-drift hurts)
  - Big new features in Employee001 until EmployeeX v0.1 stands
HARD-CONSTRAINTS:
  - No shared DB / no runtime dependency between the two products
  - AgenticMail spike: read code first, never run their curl|bash installer
  - 7-day npm supply-chain rule applies to every new dependency
  - Employee001: work on main, batch commits, one release later
</north-star>
