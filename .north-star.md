<north-star>
GOAL: Employee001 gets two operation modes — Base (twin you talk to) and EmployeeX (worker that acts unattended) — switched by a persisted sidebar toggle that gates surfaces AND behavior (off = autonomous work stops firing).
USER: Dolev, solo founder — and every npx employee001 install after the next release.
DONE-WHEN: Toggle in sidebar persists across restarts; X-mode shows Cockpit/Inbox/Audit/Budgets/Routines/Focus, Base hides them; with toggle off the routine scheduler fires nothing; lint + build green.
NON-GOALS:
  - Email layer, WorkItem queue, durable approvals (Tier 2 — separate pass, after this ships)
  - Browser-per-employee, multi-tenancy (not in v1 at all)
  - Splitting into a second repo/product — EmployeeX is a mode, not a sibling
HARD-CONSTRAINTS:
  - Tier 1 = gate existing surfaces only; no new core runtime code
  - Inline styles + CSS vars (--text/--surface/--hairline/--sp-*/--fs-*), framer-motion; no Tailwind
  - Work on main; commit freely, do NOT release (batch into one release later)
  - Mode is a workspace setting stored server-side in data/, not localStorage — it gates server behavior
</north-star>
