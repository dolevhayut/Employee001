# Dependencies & build
node_modules/
.next/
out/
*.tsbuildinfo

# Local config & secrets
.env
.env.local
.env.development
.env.production

# Local data (created on first run)
data/

# OS
.DS_Store
Thumbs.db

# Editor scratch
*.swp
.vscode/
.idea/

# Local-only docs — session summaries, build notes, scratch
# Anything under docs/local/ stays on disk only.
docs/local/
